{
	"name": "kaplay-game-engine-test",
	"type": "module",
	"scripts": {
		"start": "npm run dev",
		"build": "vite build",
		"dev": "vite --force",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
		"check": "tsc"
	},
	"dependencies": {
		"kaplay": "^3001.0.6"
	},
	"devDependencies": {
		"@types/node": "^22.10.2",
		"typescript": "^5.7.2",
		"vite": "^6.0.6"
	}
}
